"use strict";

/**
 * Drupal behavior example
 *
 * @see https://www.drupal.org/docs/drupal-apis/javascript-api/javascript-api-overview
 */
/**
(function ($, Drupal) {
  Drupal.behaviors.myModuleBehavior = {
    attach: function (context) {
      $(".element", context).hide();
    }
  };
})(jQuery, Drupal);
 */
